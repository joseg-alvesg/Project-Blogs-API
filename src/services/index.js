const userService = require('./user.service');
const categoryService = require('./categoies.service');
const blogPost = require('./post.service');

module.exports = {
  userService,
  categoryService,
  blogPost,
};