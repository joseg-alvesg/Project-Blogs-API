const loginControler = require('./login.controller');
const userControler = require('./user.controller');
const categoryController = require('./categories.controller');

module.exports = {
  loginControler,
  userControler,
  categoryController,
};